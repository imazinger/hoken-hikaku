# がん保険5商品の比較ページ

53歳・男性・喫煙あり／月3,000円程度の予算を前提に、がん保険5商品を比較する静的Webページです。
サーバー処理・APIキー・外部ライブラリを使わない、HTML・CSS・JavaScript のみの構成です。

情報確認日：**2026年9月4日**

---

## ファイル構成

```
hoken-hikaku/
├── index.html          ページ本体（文章・比較表・出典リンク）
├── assets/
│   ├── style.css       見た目の設定（色・文字サイズ・レスポンシブ）
│   └── script.js       表示の補助のみ（無効でも内容はすべて読めます）
└── README.md           このファイル
```

- リンクはすべて相対パス（`assets/style.css`）です。GitHub Pages のリポジトリ配下のURL
  （`https://<ユーザー名>.github.io/<リポジトリ名>/`）でも壊れません。
- 画像は使用していません。ローカルのファイルパスにも依存していません。

---

## 更新するときに触る場所

| 更新したいもの | 場所 |
| --- | --- |
| 情報確認日 | `index.html` の `class="updated"` が付いた行（ヘッダーとフッターの2か所） |
| 比較表の内容 | `index.html` の `<table class="cmp">`。1商品＝`<tr>`1行 |
| 比較表の列を増減する | `<thead>` の `<th>` と、各行の `<td data-label="…">` を同じ順序でそろえる（`data-label` がスマホ表示の項目名になります） |
| 各商品の詳しい説明 | `index.html` の `id="kyosai" / "sbi" / "himawari" / "taiyo-shindan" / "taiyo-chiryo"` の各 `<section>` |
| 未確認事項の一覧 | `id="unknowns"` の `<ol class="unknown-list">` |
| 出典リンク | 各商品セクション末尾の `<p class="src">` と、`id="sources"` のリンク集（2か所あります） |
| 色・文字サイズ | `assets/style.css` の先頭 `:root { … }` |
| 表がカード表示に切り替わる幅 | `assets/style.css` の `@media (max-width:860px)` と `script.js` 内の同じ数値 |

### 情報の確かさのタグ

本文中で `<span class="tag tag-○○">` を付けると、色分けされたタグが表示されます。

| 記述 | 表示 | 意味 |
| --- | --- | --- |
| `tag-official` | 公式 | 公式資料で確認できた内容 |
| `tag-heard` | 本人談 | ご本人から伺った内容 |
| `tag-unknown` | 未確認 | 公開情報からは確認できていない内容 |
| `tag-calc` | 計算 | 計算して求めた金額や年齢 |

---

## 表示の確認方法

ブラウザで `index.html` を開くだけで確認できます。ローカルサーバーを使う場合は次のとおりです。

```bash
cd hoken-hikaku
python3 -m http.server 8000
# ブラウザで http://localhost:8000/ を開く
```

確認済みの表示幅：375px（スマートフォン）／768px／1280px／1600px。
いずれの幅でもページ全体の横スクロールは発生しません。比較表は860px以下で商品ごとのカード表示に切り替わります。

---

## GitHub Pages での公開手順

公開先リポジトリ・ブランチ・独自ドメインは未指定です。決まり次第、下記の `<ユーザー名>` `<リポジトリ名>` を置き換えてください。

1. GitHub で公開用リポジトリを作成します（Public、または GitHub Pages が使えるプラン）。
2. このフォルダの中身（`index.html` と `assets/`）をリポジトリの**ルート**に置きます。
3. コミットしてプッシュします。

   ```bash
   git init                      # 既存リポジトリなら不要
   git add index.html assets README.md
   git commit -m "がん保険比較ページを追加"
   git branch -M main
   git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
   git push -u origin main
   ```

4. GitHub のリポジトリ画面で **Settings → Pages** を開きます。
5. **Source** を「Deploy from a branch」、**Branch** を `main` / `/ (root)` に設定して Save します。
6. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます。

補足

- `index.html` をルート以外（例：`docs/`）に置く場合は、手順5の Branch を `main` / `/docs` に変更してください。
- ファイル名や拡張子に日本語・大文字を使うと環境によって表示できないことがあります。半角小文字のままにしてください。
- 独自ドメインを使う場合は Settings → Pages の Custom domain に設定し、DNS 側にも設定を追加します（この資料では未設定です）。

---

## まだ確認できていないこと

1. 共済経由と聞いている任意保険の**正式商品名・引受保険会社・約款**
2. 同保険の**先進医療／自由診療の対象範囲**
3. 同保険が**実額補償か定額給付か**
4. 同保険の**支払上限・支払回数**
5. 同保険の**将来の保険料と退職後の継続条件**
6. 太陽生命の2商品（診断型・治療型）の**53歳男性の月額保険料**
7. 「**100歳時代ワクワクがん・医療保険**」という商品名と正式名称との一致
8. 「勇気のお守り」通信販売プランに**がん保険料免除特約**が含まれるかどうか

これらは推測で埋めず、ページ上でも「未確認」と明示しています。

---

## このページで意図的に避けている表現

改訂の際もご注意ください。

- 「最新治療をすべて保障」「未承認薬なら何でも対象」などの断定
- ランキング、「絶対おすすめ」「これが最強」などの表現
- 保険料だけを根拠にした優劣の判断
- 「新しい治療」「先進医療」「自由診療」を同じ意味として扱うこと
- 太陽生命の2商品を「100歳時代シリーズの2プラン」と断定すること
- 共済経由と聞いている保険を、別団体の公開プランで代用すること
- 「71歳以上は継続できない」をより具体的な条件に言い換えること
- SBI損保の年払34,400円を「月払2,886円×12」として扱うこと
- 太陽生命の「給付倍率60倍」を「60か月」と読み替えること
- がんの保障開始（②90日／③3か月／④⑤90日）を機械的に統一すること
